import { EventEmitter } from 'events';
import { ResponseStream } from './response-stream';
import { Socket } from 'net';
import { Command } from '.';
import { SentCommand } from './commands';
import { Event, EventResponse, Response } from './responses';
export type EventMatchingFunction = (event: Event) => boolean;
interface WaitingEvent {
    name: string;
    matchingFunction?: EventMatchingFunction;
    onReceived(event: object): void;
    onError(): void;
}
export declare class Connection extends EventEmitter {
    responseStream: ResponseStream;
    sentCommands: SentCommand[];
    receivedEvents: EventResponse[];
    waitingEvents: WaitingEvent[];
    socket: Socket;
    constructor(socket: Socket);
    kill(): void;
    send(command: Command): Promise<object>;
    waitForEvent(name: string, matchingFunction?: (event: object) => boolean, timeout?: number): Promise<unknown>;
    responseReceived(response: Response): void;
    eventReceived(event: EventResponse): void;
    clearEvents(): void;
    notifyWaitingEvents(): void;
}
export {};
//# sourceMappingURL=connection.d.ts.map