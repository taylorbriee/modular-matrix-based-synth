"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.launchApp = void 0;
const child_process_1 = require("child_process");
const path_1 = __importDefault(require("path"));
const fs_1 = __importDefault(require("fs"));
const nextRunId = (() => {
    let runId = 1;
    return () => {
        return runId++;
    };
})();
const createLogFiles = (logDirectory) => {
    const runId = nextRunId();
    const stdoutPath = path_1.default.join(logDirectory, `application-tests-stdout-${runId}.log`);
    const stderrPath = path_1.default.join(logDirectory, `application-tests-stderr-${runId}.log`);
    return {
        stdout: fs_1.default.createWriteStream(stdoutPath, { flags: 'a' }),
        stderr: fs_1.default.createWriteStream(stderrPath, { flags: 'a' }),
    };
};
const launchApp = ({ path, extraArgs, env, logDirectory }) => {
    const process = (0, child_process_1.spawn)(path, extraArgs, { env });
    if (logDirectory) {
        const logs = createLogFiles(logDirectory);
        process.stdout.pipe(logs.stdout);
        process.stderr.pipe(logs.stderr);
    }
    return process;
};
exports.launchApp = launchApp;
//# sourceMappingURL=app-process.js.map